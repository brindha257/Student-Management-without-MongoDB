<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Student Found</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">

    <!-- Header -->
    <header class="bg-green-600 p-6 text-white text-center">
        <h1 class="text-3xl font-bold">Student Found</h1>
        <p class="text-sm mt-2">Here are the details of the searched student</p>
    </header>

    <!-- Main Content -->
    <main class="flex-grow flex items-center justify-center p-8">
        <div class="bg-white rounded-lg shadow-lg p-8 max-w-lg w-full">
            <h2 class="text-2xl font-bold mb-6 text-center text-green-600">Student Details</h2>

            <div class="space-y-4">
                <div>
                    <p class="font-semibold">Roll Number:</p>
                    <p class="text-gray-700"><%= student.id %></p>
                </div>

                <div>
                    <p class="font-semibold">Name:</p>
                    <p class="text-gray-700"><%= student.name %></p>
                </div>

                <div>
                    <p class="font-semibold">Age:</p>
                    <p class="text-gray-700"><%= student.age %></p>
                </div>

                <div>
                    <p class="font-semibold">Grade:</p>
                    <p class="text-gray-700"><%= student.grade %></p>
                </div>

                <div>
                    <p class="font-semibold">Feedback:</p>
                    <p class="text-gray-700"><%= student.feedback %></p>
                </div>

                <% if (student.score) { %>
                <div>
                    <p class="font-semibold">Score:</p>
                    <p class="text-gray-700"><%= student.score %></p>
                </div>
                <% } %>

                <div>
                    <p class="font-semibold">Passed-out Year:</p>
                    <p class="text-gray-700"><%= student.passedYear %></p>
                </div>
            </div>

            <div class="mt-8 flex justify-center">
                <a href="/" class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-6 rounded-lg font-semibold">
                    Go Back
                </a>
            </div>
        </div>
    </main>
</body>
</html>

